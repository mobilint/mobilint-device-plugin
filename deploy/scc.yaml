# Generated from chart by make manifests. DO NOT EDIT.
---
# Source: mobilint-device-plugin/templates/scc.yaml
# OpenShift/MicroShift: allow this SA to use the privileged SCC.
# Required because the plugin runs privileged and accesses /dev/aries* via hostPath.
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: mobilint-device-plugin-scc-privileged
  namespace: kube-system
  labels:
    helm.sh/chart: mobilint-device-plugin-1.1.1
    app.kubernetes.io/name: mobilint-device-plugin
    app.kubernetes.io/instance: mobilint-device-plugin
    app.kubernetes.io/version: "v1.1.1"
    app.kubernetes.io/component: device-plugin
    app.kubernetes.io/part-of: mobilint-platform
    app.kubernetes.io/managed-by: Helm
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: system:openshift:scc:privileged
subjects:
  - kind: ServiceAccount
    name: mobilint-device-plugin
    namespace: kube-system
