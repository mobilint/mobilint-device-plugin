# Generated from chart by make manifests. DO NOT EDIT.
---
# Source: mobilint-device-plugin/templates/daemonset.yaml
apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: mobilint-device-plugin
  namespace: kube-system
  labels:
    helm.sh/chart: mobilint-device-plugin-1.0.1
    app.kubernetes.io/name: mobilint-device-plugin
    app.kubernetes.io/instance: mobilint-device-plugin
    app.kubernetes.io/version: "v1.0.1"
    app.kubernetes.io/component: device-plugin
    app.kubernetes.io/part-of: mobilint-platform
    app.kubernetes.io/managed-by: Helm
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: mobilint-device-plugin
      app.kubernetes.io/instance: mobilint-device-plugin
  updateStrategy:
    type: RollingUpdate
  template:
    metadata:
      labels:
        app.kubernetes.io/name: mobilint-device-plugin
        app.kubernetes.io/instance: mobilint-device-plugin
    spec:
      priorityClassName: system-node-critical
      tolerations:
        - operator: Exists
      nodeSelector:
        mobilint.com/npu.present: "true"
      containers:
        - name: mobilint-device-plugin
          image: "ghcr.io/mobilint/mobilint-device-plugin:v1.0.1"
          imagePullPolicy: IfNotPresent
          securityContext:
            privileged: true
            readOnlyRootFilesystem: true
          ports:
            - name: metrics
              containerPort: 9400
              protocol: TCP
          readinessProbe:
            httpGet:
              path: /readyz
              port: metrics
            periodSeconds: 5
            failureThreshold: 1
          resources:
            limits:
              cpu: 200m
              memory: 128Mi
            requests:
              cpu: 50m
              memory: 32Mi
          volumeMounts:
            - name: device-plugin
              mountPath: /var/lib/kubelet/device-plugins
            - name: cdi
              mountPath: /var/run/cdi
      volumes:
        - name: device-plugin
          hostPath:
            path: /var/lib/kubelet/device-plugins
            type: Directory
        - name: cdi
          hostPath:
            path: /var/run/cdi
            type: DirectoryOrCreate
